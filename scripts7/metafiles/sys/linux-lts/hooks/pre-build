#!/usr/bin/env bash
set -euo pipefail
: "${MAKE:=make}"
: "${NPROC:=$(getconf _NPROCESSORS_ONLN 2>/dev/null || echo 1)}"
$MAKE -j"$NPROC"
$MAKE -j"$NPROC" modules
case "${ARCH:-}" in arm|arm64) $MAKE -j"$NPROC" dtbs || true ;; esac
