#!/usr/bin/env bash
set -e

cd build

echo "[coreutils-temp] Configurando para temporary tools..."

../configure \
    --host="$ADM_TC_TARGET" \
    --build="$(../build-aux/config.guess)" \
    --prefix=/usr \
    --without-gmp \
    FORCE_UNSAFE_CONFIGURE=1
