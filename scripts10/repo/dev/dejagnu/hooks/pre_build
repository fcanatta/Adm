#!/usr/bin/env bash
# hook: pre_build para dejagnu
# Build simples autotools, depois pula fluxo padrão.

set -eu

: "${ADM_INSTALL_ROOT:=/}"
: "${ADM_BUILD_SRC_DIR:?}"

cd "${ADM_BUILD_SRC_DIR}"

./configure \
  --prefix=/usr

make
make install

export ADM_BUILD_SKIP_DEFAULT=1
