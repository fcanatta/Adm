#!/usr/bin/env bash
# Wrapper de pós-instalação para o hook unificado do musl
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export MUSL_HOOK_PHASE="post"

exec "${SCRIPT_DIR}/musl.security_hook"
