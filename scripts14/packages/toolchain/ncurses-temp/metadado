name: ncurses-temp
version: 6.5-20250809
release: 1

category: toolchain
license: MIT
url: https://invisible-island.net/ncurses/

# Ajuda o adm-build/adm-profile a escolher profile toolchain + glibc
libc: glibc

summary: Ncurses 6.5-20250809 temporário (cross) para o LFS (cap. 6.3)
description: >
  Ncurses 6.5-20250809 construída como ferramenta temporária cross,
  seguindo o capítulo 6.3 do LFS (Cross Compiling Temporary Tools).
  Inclui build de tic nativo em $LFS/tools e bibliotecas compartilhadas
  wide-char, instaladas via DESTDIR e integradas ao pipeline ADM 2.0.

# Depende da toolchain cross e da libc já instalada
build_deps: toolchain/m4 toolchain/gcc-pass1 toolchain/glibc toolchain/gcc-libstdc++
run_deps:
opt_deps:

# groups: para build-group, relatórios e heurísticas de kind
groups: toolchain temp-tools cross-temp ncurses

# Fonte oficial para esse snapshot (tarball corrente do ncurses 6.5-20250809)
# LFS usa esse snapshot da branch 6.5. 
source_uri: https://invisible-island.net/archives/ncurses/current/ncurses-6.5-20250809.tgz

# Deixa o sha256 em branco para você preencher depois se quiser
source_checksum:
