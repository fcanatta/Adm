name: linux-headers
version: 6.17.8
release: 1
category: toolchain
license: GPL-2.0-only
url: https://www.kernel.org/

# Ajuda o adm-build a escolher profile (toolchain + glibc)
libc: glibc

summary: Linux API Headers 6.17.8 para o toolchain (LFS 5.4)
description: Headers de API do kernel Linux 6.17.8, sanitizados para uso pelo Glibc no estágio de cross-toolchain (Capítulo 5.4 do LFS).

# Lidos por adm-deps / adm-build
# Ordem LFS: binutils-pass1 -> gcc-pass1 -> linux-headers -> glibc -> libstdc++
build_deps: toolchain/binutils-pass1 toolchain/gcc-pass1
run_deps:
opt_deps:

# groups: usado pela heurística de "kind" toolchain, build-group e relatórios
groups: toolchain toolchain-bootstrap linux headers api pass1

# Fonte oficial (LFS usa este tarball)
# https://www.kernel.org/pub/linux/kernel/v6.x/linux-6.17.8.tar.xz
source_uri: https://www.kernel.org/pub/linux/kernel/v6.x/linux-6.17.8.tar.xz

# SHA256 pode ser preenchido depois:
# linux-6.17.8.tar.xz sha256 = ...
source_checksum:
