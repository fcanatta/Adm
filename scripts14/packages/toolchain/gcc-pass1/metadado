name: gcc-pass1
version: 14.2.0
release: 1
category: toolchain
license: GPL-3.0-or-later

url: https://gcc.gnu.org/

# glibc é o alvo final do sistema, mesmo que esse GCC ainda seja "cross"
libc: glibc

summary: GCC cross (pass 1) para bootstrap do toolchain
description: >
  GCC pass1: compilador C cross usado para construir temporariamente
  o toolchain inicial antes da libc final, seguindo o esquema LFS.

# Dependências de build na visão do ADM.
# Tipicamente:
#   - toolchain/binutils-pass1
#   - toolchain/linux-headers      (às vezes vem depois, dependendo da pipeline)
#   - toolchain/mpfr, gmp, mpc... (se você modelar como pacotes separados)
build_deps: toolchain/binutils-pass1

run_deps:
opt_deps:

# Ajuda o ADM a classificar e fazer build-group/upgrade correto.
groups: toolchain toolchain-bootstrap cross gcc pass1

# Fonte oficial (exemplo LFS 12.2)
# Download: https://ftp.gnu.org/gnu/gcc/gcc-14.2.0/gcc-14.2.0.tar.xz
source_uri: https://ftp.gnu.org/gnu/gcc/gcc-14.2.0/gcc-14.2.0.tar.xz

# SHA256 de gcc-14.2.0.tar.xz
# (exemplo: ports de FreeBSD registram SHA256 a7b39b... para esse tarball) 
source_checksum: a7b39bc69cbf9e25826c5a60ab26477001f7c08d85cec04bc0e29cabed6f3cc9
