name: perl-temp
version: 5.36.0
release: 1

category: toolchain
license: GPL-1.0-or-later & Artistic-1.0-Perl
url: https://www.perl.org/

libc: glibc

summary: Perl 5.36.0 temporário para LFS (cap. 7.9, chroot)
description: >
  Perl 5.36.0 construído como ferramenta temporária dentro do chroot,
  seguindo o capítulo 7.9 do LFS. Configurado com sh Configure -des e
  diretórios de libs/perl5 iguais ao livro, integrado ao pipeline ADM 2.0
  usando DESTDIR para instalar em /usr dentro do sysroot.

# Já estamos no chroot com toolchain Pass 2 funcional
build_deps: toolchain/gcc-pass2 toolchain/binutils-pass2 toolchain/glibc toolchain/make-temp toolchain/coreutils-temp toolchain/sed-temp toolchain/bash-temp
run_deps:
opt_deps:

# groups para build-group e relatórios
groups: toolchain temp-tools chroot perl

# Fonte oficial usada no LFS 11.x / 12.x
source_uri: https://www.cpan.org/src/5.0/perl-5.36.0.tar.xz

# pode ficar em branco e ser preenchido depois
source_checksum:
