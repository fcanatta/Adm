name: sed-temp
version: 4.9
release: 1

category: toolchain
license: GPL-3.0-or-later
url: https://www.gnu.org/software/sed/

# ajuda o adm-build/adm-profile a escolher profile toolchain + glibc
libc: glibc

summary: Sed 4.9 temporário (cross) para o LFS (cap. 6.14)
description: >
  Sed 4.9 construído como ferramenta temporária cross, seguindo o
  capítulo 6.14 do LFS (Cross Compiling Temporary Tools). Usa
  --host=$LFS_TGT e --build=$(./build-aux/config.guess),
  integrado ao pipeline do ADM 2.0 com DESTDIR.

# depende da toolchain temporária já funcional
build_deps: toolchain/patch-temp toolchain/make-temp toolchain/gzip-temp toolchain/grep-temp toolchain/gawk-temp toolchain/findutils-temp toolchain/coreutils-temp toolchain/bash-temp toolchain/gcc-pass1 toolchain/glibc
run_deps:
opt_deps:

# groups: para build-group temp-tools, relatórios, etc.
groups: toolchain temp-tools cross-temp sed

# Tarball oficial 4.9 (igual livro)
source_uri: https://ftp.gnu.org/gnu/sed/sed-4.9.tar.xz

# pode preencher depois com o sha256 oficial se quiser
source_checksum:
