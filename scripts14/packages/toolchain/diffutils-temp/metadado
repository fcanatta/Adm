name: diffutils-temp
version: 3.12
release: 1

category: toolchain
license: GPL-3.0-or-later
url: https://www.gnu.org/software/diffutils/

# Ajuda o adm-build/adm-profile a escolher o profile correto
libc: glibc

summary: Diffutils 3.12 temporário (cross) para o LFS (cap. 6.6)
description: >
  Diffutils 3.12 construído como ferramenta temporária cross, seguindo o
  capítulo 6.6 do LFS (Cross Compiling Temporary Tools), usando
  --host=$LFS_TGT, --build=$(./build-aux/config.guess) e
  gl_cv_func_strcasecmp_works=y, integrado ao pipeline do ADM 2.0.

# Depende da toolchain temporária já estar funcional
build_deps: toolchain/coreutils-temp toolchain/bash-temp toolchain/gcc-pass1 toolchain/glibc
run_deps:
opt_deps:

# groups: para build-group (ex: temp-tools), relatórios, etc.
groups: toolchain temp-tools cross-temp diffutils

# Tarball do livro r12.4-systemd
source_uri: https://ftp.gnu.org/gnu/diffutils/diffutils-3.12.tar.xz

# Você pode preencher depois com o sha256 real:
source_checksum:
