name: binutils-pass1
version: 2.45.1
release: 1

category: toolchain
license: GPL-3.0-or-later
url: https://www.gnu.org/software/binutils/

# Ajuda o adm-build a escolher o profile (toolchain + glibc)
libc: glibc

summary: Binutils cross (pass 1) para bootstrap do toolchain
description: Binutils pass1: linker/assembler e ferramentas para o cross-toolchain inicial, usados antes da libc final.

# Lidos por adm-deps / adm-build
# No LFS, binutils-pass1 é o primeiro pacote do toolchain, então normalmente
# não há deps de build aqui – se você tiver um "host-base" pode colocar depois.
build_deps:
run_deps:
opt_deps:

# groups: usado pela heurística de "kind" (toolchain) e por build-group, relatórios etc
groups: toolchain toolchain-bootstrap cross binutils pass1

# Fonte oficial (alinhado com LFS "development")
# Veja LFS capítulo 3 – lista de pacotes (Binutils 2.45.1, download .tar.xz).
# https://www.linuxfromscratch.org/lfs/view/development/chapter03/packages.html
# Download:
#   https://sourceware.org/pub/binutils/releases/binutils-2.45.1.tar.xz
source_uri: https://sourceware.org/pub/binutils/releases/binutils-2.45.1.tar.xz

# SHA256 puro (sem prefixo), para o adm-fetch:
# Pode deixar em branco por enquanto como você pediu.
source_checksum:
