name: tar-temp
version: 1.35
release: 1

category: toolchain
license: GPL-3.0-or-later
url: https://www.gnu.org/software/tar/

libc: glibc

summary: Tar 1.35 temporário (cross) para o LFS (cap. 6.15)
description: >
  Tar 1.35 construído como ferramenta temporária cross, seguindo o
  capítulo 6.15 do LFS (Cross Compiling Temporary Tools). Usa
  --host=$LFS_TGT e --build=$(build-aux/config.guess), integrado
  ao pipeline do ADM 2.0 com DESTDIR.

# depende da toolchain temporária já funcional
build_deps: toolchain/sed-temp toolchain/patch-temp toolchain/make-temp toolchain/gzip-temp toolchain/grep-temp toolchain/gawk-temp toolchain/findutils-temp toolchain/coreutils-temp toolchain/bash-temp toolchain/gcc-pass1 toolchain/glibc
run_deps:
opt_deps:

# groups: para build-group temp-tools, relatórios, etc.
groups: toolchain temp-tools cross-temp tar

# Tarball oficial 1.35 usado no LFS 12.4
source_uri: https://ftp.gnu.org/gnu/tar/tar-1.35.tar.xz

# pode preencher depois com o sha256 se quiser
source_checksum:
