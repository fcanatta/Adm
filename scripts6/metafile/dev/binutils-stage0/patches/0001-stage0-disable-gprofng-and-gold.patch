diff --git a/configure b/configure
index 1111111..2222222 100755
--- a/configure
+++ b/configure
@@ -1,6 +1,36 @@
 #! /bin/sh
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.7x.
+
+# === ADM STAGE0 PATCH =========================================
+# Força build mínimo no stage0:
+#  - Desabilita gprofng e gold por padrão (mesmo se o tarball vier habilitando)
+#  - Em hosts musl, gprofng não é suportado → força disable
+#  - Pode ser reabilitado explicitamente com --enable-gprofng / --enable-gold
+#  - Respeita sinalizador externo ADM_STAGE0=1 (bootstrap)
+# ==============================================================
+
+# Captura argv bruto para ver se o usuário pediu enable explícito
+adm_explicit_enable_gprofng=no
+adm_explicit_enable_gold=no
+for opt in "$@"; do
+  case "$opt" in
+    --enable-gprofng) adm_explicit_enable_gprofng=yes ;;
+    --enable-gold)    adm_explicit_enable_gold=yes ;;
+  esac
+done
+
+# Detecta musl de forma simples (host triplet ou libc)
+adm_is_musl=no
+case "${host:-${HOSTTYPE}-${OSTYPE}}" in
+  *musl*|*linux-musl*) adm_is_musl=yes ;;
+esac
+
+# Se stage0 ou musl, força defaults conservadores
+if test "x${ADM_STAGE0}" = "x1" -o "x$adm_is_musl" = "xyes"; then
+  test "x$adm_explicit_enable_gprofng" = "xyes" || set -- "$@" --disable-gprofng
+  test "x$adm_explicit_enable_gold"    = "xyes" || set -- "$@" --disable-gold
+fi
+# === /ADM STAGE0 PATCH ========================================
 
 # Initialize some variables set by options.
 ac_init_help=
diff --git a/Makefile.in b/Makefile.in
index 3333333..4444444 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -1,10 +1,33 @@
 # Top-level Makefile template
 # Generated by automake
 
+# === ADM STAGE0 PATCH =========================================
+# Remove subdirs problemáticos no stage0 quando explicitamente solicitado.
+# Não altera o comportamento padrão de quem pedir --enable-gprofng/--enable-gold.
+# Em ambientes onde o configure já remove, isto é no-op.
+# ==============================================================
+
+ifneq ($(ADM_STAGE0),)
+# Alguns tarballs listam diretórios diretamente na variável SUBDIRS (ou similares).
+# Fazemos uma filtragem defensiva para evitar entrar em gprofng e gold no stage0.
+override SUBDIRS := $(filter-out gprofng gold,$(SUBDIRS))
+endif
+
+# =================================================================
 # Subdirectories to build
-SUBDIRS = @SUBDIRS@
+SUBDIRS = @SUBDIRS@
 
 all-recursive:
 	@for dir in $(SUBDIRS); do \
 	   (cd $$dir && $(MAKE) $(AM_MAKEFLAGS) all) || exit $$?; \
 	done
+
+# === /ADM STAGE0 PATCH =========================================
