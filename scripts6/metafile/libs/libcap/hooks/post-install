#!/usr/bin/env bash
set -Eeuo pipefail
: "${ADM_WORK_DIR:?}" "${ADM_DESTDIR:?}"
cd "$ADM_WORK_DIR"
make -C lib   DESTDIR="$ADM_DESTDIR" prefix=/usr install
make -C progs DESTDIR="$ADM_DESTDIR" prefix=/usr install
rm -rf "$ADM_DESTDIR/usr/share/doc" "$ADM_DESTDIR/usr/share/info" 2>/dev/null || true
