# Overlay do GCC pass2 (bootstrap): C e C++ com headers já disponíveis.
# Objetivo: gerar compilador target-aware usando a libc do stage.

name=gcc
version=14.2.0
category=dev
build_type=autotools
run_deps=
build_deps=
opt_deps=
num_builds=0
description=GNU Compiler Collection (bootstrap pass2: C/C++ target-aware)
homepage=https://gcc.gnu.org/
maintainer=ADM Packaging <adm@example.local>

# Mantém apenas o tarball do gcc; prereqs (gmp/mpfr/mpc/isl) via vendor/download_prerequisites nos hooks.
sha256sums=
sources=https://ftp.gnu.org/gnu/gcc/gcc-14.2.0/gcc-14.2.0.tar.xz
