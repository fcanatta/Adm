# 1) Carrega ambiente
source ./profile-glibc.sh   # define ADM_ROOTFS, TARGET_TRIPLET, etc.

# 2) Cabe lembrar: já deve existir:
#    - binutils-2.45.1-pass1 instalado no mesmo ADM_ROOTFS
#    - gcc-15.2.0-pass1 instalado apontando para o mesmo TARGET_TRIPLET/CROSS_SYSROOT
#    - Linux-6.17.9 API headers instalados em ${ADM_ROOTFS}/usr/include

# 3) Build do pacote glibc pass1 (gera tar.xz no cache do adm)
ADM_ROOTFS=/opt/systems/glibc-root \
  ./admV2.sh build glibc-2.42-pass1

# 4) Instala glibc pass1 no root do sistema glibc
ADM_ROOTFS=/opt/systems/glibc-root \
  ./admV2.sh install glibc-2.42-pass1
