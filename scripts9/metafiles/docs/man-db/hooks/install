#!/usr/bin/env bash
set -Eeuo pipefail
: "${WORKDIR:?}"; : "${DESTDIR:?}"

cd "$WORKDIR"
srcdir="$(find . -maxdepth 1 -type d -name 'man-db-*' | head -n1 || true)"
[[ -d "$srcdir" ]] || { echo "[ERR] man-db: diretório man-db-* não encontrado em $WORKDIR" >&2; exit 3; }

cd "$srcdir"
make DESTDIR="$DESTDIR" install
