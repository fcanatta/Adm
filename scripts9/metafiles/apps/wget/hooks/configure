#!/usr/bin/env bash
set -Eeuo pipefail
: "${WORKDIR:?}"

cd "$WORKDIR"
dir=$(find . -maxdepth 1 -type d -name 'wget-*' | head -n1)
cd "$dir"

./configure \
  --prefix=/usr \
  --with-ssl=openssl \
  --with-libpcre2
